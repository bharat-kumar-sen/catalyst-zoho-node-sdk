import { PasswordPatternDirective } from "./password-pattern.directive";

describe('PasswordPatternDirective', () => {
  it('should create an instance', () => {
    const directive = PasswordPatternDirective;
    expect(directive).toBeTruthy();
  });
});
